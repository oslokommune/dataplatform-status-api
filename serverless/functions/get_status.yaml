handler: status.get_status.handler
environment:
  STAGE: ${self:provider.stage}
events:
- http:
    path: status/{statusid}
    method: get
    cors: { origin: '*' }
    request:
      parameters:
        paths:
          statusid: true
