handler: status.get_status.handler
environment:
  STAGE: ${self:provider.stage}
events:
- http:
    path: status/{uuid}
    method: get
    cors: { origin: '*' }
    request:
      parameters:
        paths:
          uuid: true
