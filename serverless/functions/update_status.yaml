handler: status.update_status.handler
environment:
  STAGE: ${self:provider.stage}
events:
- http:
    path: status/{statusid}
    method: post
    cors: { origin: '*' }
    request:
      parameters:
        paths:
          statusid: true