handler: status.update_status.handler
environment:
  STAGE: ${self:provider.stage}
events:
- http:
    path: status/{uuid}/{nyStatus}
    method: put
    cors: { origin: '*' }
    request:
      parameters:
        paths:
          uuid: true