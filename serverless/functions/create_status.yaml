handler: status.create_status.handler
environment:
  STAGE: ${self:provider.stage}
events:
- http:
    path: status/{uuid}
    method: post
    cors: { origin: '*' }
    request:
      parameters:
        paths:
          uuid: true
